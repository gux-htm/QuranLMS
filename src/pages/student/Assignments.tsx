import { useMemo, useState } from 'react'
import { BookOpen, Inbox } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
import { Button, Card, CardContent, CardTitle } from '@/components/ui'
type Assignment={id:string;title:string;assigned:string;deadline:string;status:'Pending'|'Completed';score?:number}
const assignments:Assignment[]=[{id:'a1',title:'Juz 1 · Pages 7–9',assigned:'Today',deadline:'Tomorrow',status:'Pending'},{id:'a2',title:'Juz 1 · Pages 4–6',assigned:'Aug 25',deadline:'Aug 28',status:'Completed',score:92},{id:'a3',title:'Noorani Qaida · Lesson 12',assigned:'Aug 20',deadline:'Aug 24',status:'Pending'}]
export function StudentAssignments(){const [tab,setTab]=useState<'Pending'|'Completed'|'All'>('Pending');const navigate=useNavigate();const list=useMemo(()=>tab==='All'?assignments:assignments.filter(a=>a.status===tab),[tab]);return <div className="space-y-6"><div><h1 className="font-display text-2xl font-semibold text-ink">Assignments</h1><p className="mt-1 text-sm text-ink/55">Your lessons and practice work.</p></div><div className="inline-flex rounded-md bg-paper-dim p-1">{(['Pending','Completed','All'] as const).map(t=><button key={t} onClick={()=>setTab(t)} className={`rounded px-3 py-1.5 text-sm ${tab===t?'bg-white text-green-700 shadow-sm':'text-ink/55'}`}>{t}</button>)}</div>{list.length===0?<Card><CardContent className="py-12 text-center"><Inbox className="mx-auto h-8 w-8 text-ink/35"/><CardTitle className="mt-3">Nothing here yet</CardTitle><p className="mt-1 text-sm text-ink/55">Your teacher will add lessons when they are ready.</p></CardContent></Card>:<div className="space-y-3">{list.map(a=><Card key={a.id}><CardContent className="flex flex-col gap-4 py-4 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><BookOpen className="h-4 w-4 text-green-700"/><h2 className="font-medium text-ink">{a.title}</h2></div><p className="mt-1 text-xs text-ink/55">Assigned {a.assigned} · Due {a.deadline}</p></div><div className="flex items-center gap-3"><span className={`rounded-full px-2.5 py-1 text-xs ${a.status==='Completed'?'bg-green-50 text-green-700':'bg-gold-100 text-gold-800'}`}>{a.status}{a.score?` · ${a.score}/100`:''}</span><Button variant="secondary" onClick={()=>navigate('/student/lesson')}>Open lesson</Button></div></CardContent></Card>)}</div>}</div>}
