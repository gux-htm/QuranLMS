import { useState } from 'react'
import { Button } from '@/components/ui/Button'
import { Card, CardContent, CardTitle } from '@/components/ui/Card'
import { Input } from '@/components/ui/Input'
import { useToast } from '@/components/ui/Toaster'
import { CURRENT_STUDENT } from '@/lib/mockData'
export function StudentSettings(){const {push}=useToast();const [language,setLanguage]=useState('English');const [notifications,setNotifications]=useState(true);return <div className="space-y-6"><div><h1 className="font-display text-2xl font-semibold text-ink">Settings</h1><p className="mt-1 text-sm text-ink/55">Manage your preferences and account security.</p></div><Card><CardContent><CardTitle>Profile</CardTitle><p className="mt-4 text-sm text-ink"><span className="text-ink/55">Name: </span>{CURRENT_STUDENT.name}</p><p className="mt-2 text-sm text-ink/55">Contact your teacher to update your display name.</p></CardContent></Card><Card><CardContent><CardTitle>Preferences</CardTitle><label className="mt-4 block text-sm font-medium">Language</label><select value={language} onChange={e=>setLanguage(e.target.value)} className="mt-1 w-full rounded-md border border-line bg-white px-3 py-2 text-sm"><option>English</option><option>Arabic</option><option>Urdu</option></select><label className="mt-4 flex items-center justify-between text-sm"><span>Notifications</span><input type="checkbox" checked={notifications} onChange={e=>setNotifications(e.target.checked)}/></label><Button className="mt-5" onClick={()=>push('Preferences saved')}>Save preferences</Button></CardContent></Card><Card><CardContent><CardTitle>Security</CardTitle><PasswordForm/></CardContent></Card></div>}
function PasswordForm(){const {push}=useToast();const [current,setCurrent]=useState('');const [next,setNext]=useState('');return <div className="mt-4 space-y-3"><Input type="password" placeholder="Current password" value={current} onChange={e=>setCurrent(e.target.value)}/><Input type="password" placeholder="New password" value={next} onChange={e=>setNext(e.target.value)}/><Button variant="outline" onClick={()=>{if(!current||next.length<8){push('Complete the fields and use at least 8 characters');return}push('Password changed')}}>Change password</Button></div>}
