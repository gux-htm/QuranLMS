import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { CheckCircle2 } from 'lucide-react'
import { Button, Card, CardContent, CardTitle, Input } from '@/components/ui'
import { useToast } from '@/components/ui/Toaster'
export function ResetPassword(){const [password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[loading,setLoading]=useState(false),[done,setDone]=useState(false);const {push}=useToast();const navigate=useNavigate();const score=(password.length>=12?2:password.length>=8?1:0)+( /[A-Z]/.test(password)&&/[0-9]/.test(password)?1:0);const label=score>=3?'Strong':score>=2?'Fair':'Weak';const submit=async(e:React.FormEvent)=>{e.preventDefault();if(password.length<8){push('Password must be at least 8 characters');return}if(password!==confirm){push('Passwords do not match');return}setLoading(true);await new Promise(r=>setTimeout(r,500));setLoading(false);setDone(true);push('Password updated');window.setTimeout(()=>navigate('/login'),2000)};return <main className="min-h-screen bg-paper px-4 py-16"><Card className="mx-auto max-w-md"><CardContent className="py-8">{done?<div className="space-y-4 text-center"><CheckCircle2 className="mx-auto h-12 w-12 text-green-700"/><CardTitle>Password reset</CardTitle><p className="text-sm text-ink/55">Redirecting to login…</p></div>:<form onSubmit={submit} className="space-y-4"><CardTitle>Set a new password</CardTitle><Input label="New password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/><div><div className="mb-1 flex justify-between text-xs text-ink/55"><span>Strength</span><span>{label}</span></div><div className="h-1.5 rounded bg-paper-dim"><div className="h-full rounded bg-green-700 transition-all" style={{width:`${Math.max(20,score*33)}%`}}/></div></div><Input label="Confirm password" type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/><Button type="submit" disabled={loading}>{loading?'Saving…':'Reset password'}</Button></form>}</CardContent></Card></main>}
